# Heavy local-only archive material — Dropbox is its backup, not git.
# (Text and code in archive/ ARE tracked: handoffs, sandbox builds, studies.)
archive/sessions/
archive/site-snapshots/
archive/reference/

# Zips and OS noise
*.zip
.DS_Store
